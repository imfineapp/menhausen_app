# 📋 План поиска и исправления захардкоженных текстовых блоков (i18n миграция)

## 🎯 Цель
Найти все захардкоженные текстовые блоки в приложении и заменить их на систему i18n для поддержки мультиязычности.

## 📊 Текущее состояние

### ✅ Что уже есть:
- **Система i18n**: `LanguageContext` и `ContentContext`
- **Файлы переводов**: `data/content/{ru,en}/ui.json` и другие JSON файлы
- **Хуки локализации**: 
  - `useLanguage()` - для получения текущего языка
  - `useContent()` - для доступа к контенту
  - `useUIText()` - для UI текстов из JSON
- **ESLint правило**: `eslint-rules/no-hardcoded-strings.js` (требует настройки)
- **Скрипт поиска**: `scripts/find-hardcoded-values.js` (ищет цвета/размеры, не тексты)

### ❌ Что нужно сделать:
1. Создать скрипт для поиска захардкоженных текстов
2. Найти все захардкоженные строки в компонентах
3. Классифицировать найденные строки
4. Добавить недостающие ключи в JSON файлы переводов
5. Заменить захардкоженные строки на i18n вызовы
6. Настроить ESLint правило для предотвращения новых захардкоженных строк

---

## 📝 Этап 1: Создание инструментов для поиска

### 1.1. Создать скрипт для поиска захардкоженных текстов
**Файл**: `scripts/find-hardcoded-texts.js`

**Функционал**:
- Поиск строковых литералов в JSX (текст между тегами)
- Поиск строк в атрибутах: `placeholder`, `title`, `alt`, `aria-label`, `label`
- Поиск строк в `getText()` вызовах (уже частично локализовано)
- Исключение технических строк (URLs, пути, классы CSS, data-testid)
- Исключение тестовых файлов
- Группировка результатов по файлам и типам

**Паттерны для поиска**:
```javascript
// JSX текст
<div>Захардкоженный текст</div>

// Атрибуты
<input placeholder="Введите текст" />
<button aria-label="Кнопка" />

// Строковые литералы в JSX выражениях
{someCondition ? 'Русский текст' : 'English text'}

// Прямые строки в props
<Component title="Заголовок" />
```

### 1.2. Улучшить ESLint правило
**Файл**: `eslint-rules/no-hardcoded-strings.js`

**Улучшения**:
- Добавить проверку строк в JSX тексте
- Добавить проверку строк в атрибутах компонентов
- Исключить уже локализованные строки (через `getText`, `getLocalizedText`, `useUIText`)
- Добавить автоматическое предложение ключа для ui.json
- Настроить в `.eslintrc` для автоматической проверки

---

## 🔍 Этап 2: Поиск захардкоженных текстов

### 2.1. Запустить скрипт поиска
```bash
node scripts/find-hardcoded-texts.js
```

### 2.2. Анализ результатов
**Категории для классификации**:

1. **UI тексты** (кнопки, заголовки, сообщения)
   - Должны быть в `data/content/{lang}/ui.json`
   - Использовать `useUIText()` или `getText(ru, en)`

2. **Контент из JSON** (уже должен быть локализован)
   - Проверить, что используется `getLocalizedText()`

3. **Динамические тексты** (сообщения об ошибках, уведомления)
   - Добавить в `ui.json` в соответствующие секции

4. **Технические строки** (исключить из обработки)
   - URLs, пути к файлам
   - CSS классы, data-атрибуты
   - Идентификаторы, ключи

### 2.3. Создать отчет
**Файл**: `i18n-migration-report.md`

**Структура отчета**:
- Список файлов с захардкоженными строками
- Количество найденных строк по категориям
- Приоритеты исправления (критичные/важные/низкие)
- Предложения по ключам для ui.json

---

## 📦 Этап 3: Подготовка переводов

### 3.1. Анализ структуры ui.json
**Проверить**:
- Все ли секции присутствуют в обоих языках (ru, en)
- Соответствие структуры между ru/ui.json и en/ui.json
- Наличие fallback значений в `ContentContext.tsx`

### 3.2. Добавить недостающие ключи
**Процесс**:
1. Для каждой найденной строки определить категорию
2. Предложить ключ в структуре ui.json
3. Добавить переводы в `data/content/ru/ui.json`
4. Добавить переводы в `data/content/en/ui.json`
5. Обновить типы в `types/content.ts` (если нужно)
6. Обновить fallback в `ContentContext.tsx` (если нужно)

**Пример структуры**:
```json
{
  "ui": {
    "navigation": { ... },
    "common": { ... },
    "home": { ... },
    "profile": { ... },
    "cards": { ... },
    "errors": { ... },
    "notifications": { ... }
  }
}
```

---

## 🔧 Этап 4: Рефакторинг компонентов

### 4.1. Приоритизация файлов
**Критерии приоритета**:
1. **Высокий**: Компоненты, видимые пользователю (HomeScreen, ProfileScreen, etc.)
2. **Средний**: Вспомогательные компоненты (модальные окна, формы)
3. **Низкий**: Утилиты, внутренние компоненты

### 4.2. Процесс рефакторинга для каждого файла

#### Шаг 1: Импорт необходимых хуков
```typescript
import { useLanguage } from '../LanguageContext';
import { useContent, useUIText } from '../ContentContext';
```

#### Шаг 2: Добавить функцию getText (если нужна)
```typescript
const { language } = useLanguage();
const getText = (ruText: string, enText: string) => {
  return language === 'ru' ? ruText : enText;
};
```

#### Шаг 3: Заменить захардкоженные строки

**Для UI текстов из ui.json**:
```typescript
// ❌ Было:
<button>Сохранить</button>

// ✅ Стало:
const ui = useUIText();
<button>{ui.common.save}</button>
```

**Для UI текстов, которых нет в ui.json**:
```typescript
// ❌ Было:
<h1>Заголовок</h1>

// ✅ Стало:
<h1>{getText('Заголовок', 'Title')}</h1>
```

**Для контента из JSON**:
```typescript
// ❌ Было:
<p>{cardData.title}</p>

// ✅ Стало:
const { getLocalizedText } = useContent();
<p>{getLocalizedText(cardData.title)}</p>
```

#### Шаг 4: Проверка
- Запустить линтер: `npm run lint`
- Проверить типы: `npm run type-check`
- Запустить тесты: `npm run test:run`
- Проверить визуально в приложении

### 4.3. Список файлов для проверки (приоритетные)

**Высокий приоритет**:
- `components/HomeScreen.tsx`
- `components/UserProfileScreen.tsx`
- `components/AppSettingsScreen.tsx`
- `components/CheckInScreen.tsx`
- `components/ThemeHomeScreen.tsx`
- `components/CardDetailsScreen.tsx`
- `components/ArticleScreen.tsx`
- `components/BadgesScreen.tsx`

**Средний приоритет**:
- `components/OnboardingScreen01.tsx`
- `components/OnboardingScreen02.tsx`
- `components/LanguageModal.tsx`
- `components/PinSetupScreen.tsx`
- `components/DeleteAccountScreen.tsx`
- `components/DonationsScreen.tsx`
- `components/PaymentsScreen.tsx`
- `components/PrivacyPolicyScreen.tsx`
- `components/TermsOfUseScreen.tsx`

**Низкий приоритет**:
- Все остальные компоненты в `components/`
- Компоненты в `components/ui/`
- Компоненты в `components/mental-techniques/`

---

## ✅ Этап 5: Валидация и тестирование

### 5.1. Проверка линтера
```bash
npm run lint
```
- Убедиться, что ESLint правило работает
- Исправить все предупреждения

### 5.2. Проверка типов
```bash
npm run type-check
```
- Убедиться, что все типы корректны
- Исправить ошибки TypeScript

### 5.3. Запуск тестов
```bash
npm run test:run
npm run test:e2e
```
- Убедиться, что все тесты проходят
- Обновить тесты, если они проверяют захардкоженные строки

### 5.4. Ручное тестирование
- Проверить приложение на русском языке
- Проверить приложение на английском языке
- Проверить переключение языков
- Проверить все основные экраны

### 5.5. Проверка покрытия
- Убедиться, что все найденные строки заменены
- Запустить скрипт поиска еще раз для проверки

---

## 🛡️ Этап 6: Предотвращение регрессий

### 6.1. Настроить ESLint правило в `.eslintrc`
```json
{
  "rules": {
    "local/no-hardcoded-strings": ["error", {
      "allowedPatterns": ["console.log", "className", "data-testid"],
      "ignoreFiles": ["**/*.test.tsx", "**/*.spec.tsx"]
    }]
  }
}
```

### 6.2. Добавить pre-commit hook (опционально)
- Проверять наличие захардкоженных строк перед коммитом
- Использовать husky или similar

### 6.3. Обновить документацию
- Обновить `guidelines/multilingual-guidelines.md` с примерами
- Добавить чек-лист для новых компонентов

---

## 📊 Метрики успеха

### Количественные:
- ✅ 0 захардкоженных пользовательских строк в компонентах
- ✅ 100% покрытие UI текстов через i18n
- ✅ Все тесты проходят
- ✅ 0 ошибок линтера

### Качественные:
- ✅ Приложение полностью поддерживает переключение языков
- ✅ Все тексты корректно отображаются на обоих языках
- ✅ Нет визуальных багов после миграции
- ✅ Код соответствует стандартам проекта

---

## 🚀 Порядок выполнения

1. **Этап 1** (1-2 часа): Создание инструментов
2. **Этап 2** (1 час): Поиск и анализ
3. **Этап 3** (2-3 часа): Подготовка переводов
4. **Этап 4** (8-12 часов): Рефакторинг компонентов (можно разбить на части)
5. **Этап 5** (2-3 часа): Валидация и тестирование
6. **Этап 6** (1 час): Настройка предотвращения регрессий

**Общее время**: ~15-22 часа работы

---

## 📝 Чек-лист для каждого компонента

При рефакторинге каждого компонента проверьте:

- [ ] Импортированы необходимые хуки (`useLanguage`, `useContent`, `useUIText`)
- [ ] Все JSX тексты заменены на i18n вызовы
- [ ] Все атрибуты (`placeholder`, `title`, `alt`, `aria-label`) используют i18n
- [ ] Все строки в условных выражениях используют i18n
- [ ] Все переводы добавлены в `ui.json` (ru и en)
- [ ] Типы обновлены (если нужно)
- [ ] Fallback обновлен в `ContentContext.tsx` (если нужно)
- [ ] Компонент протестирован на обоих языках
- [ ] Линтер не выдает ошибок
- [ ] TypeScript не выдает ошибок

---

## 🔗 Полезные ссылки

- **Документация i18n**: `guidelines/multilingual-guidelines.md`
- **Контексты**: 
  - `components/LanguageContext.tsx`
  - `components/ContentContext.tsx`
- **Переводы**: `data/content/{ru,en}/ui.json`
- **Типы**: `types/content.ts`
- **ESLint правило**: `eslint-rules/no-hardcoded-strings.js`

---

## 💡 Рекомендации

1. **Работайте итеративно**: Рефакторите по одному компоненту, тестируйте, коммитьте
2. **Используйте ветки**: Создайте отдельную ветку для миграции
3. **Документируйте**: Записывайте найденные проблемы и решения
4. **Тестируйте часто**: Проверяйте приложение после каждого значительного изменения
5. **Следуйте стандартам**: Используйте существующие паттерны локализации

---

**Дата создания плана**: 2024
**Статус**: Готов к выполнению


